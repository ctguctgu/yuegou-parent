spring:
  cloud:
    config:
      profile: dev
      name: application-product
      discovery:
        service-id: YUEGOU-CONFIG
        enabled: true
#注册中心配置
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://root:821096@localhost:8761/eureka/
logging:
  level:
    cn.it.yuegou: debug